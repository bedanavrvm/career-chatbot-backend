from django.apps import AppConfig


class ConversationsConfig(AppConfig):
    """Django AppConfig for the conversations app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'conversations'
